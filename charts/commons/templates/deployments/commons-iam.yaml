kind: Deployment
apiVersion: apps/v1
metadata:
  name: mechcloud-commons-iam
spec:
  replicas: {{ .Values.global.replicaCount }}
  selector:
    matchLabels:
      app: mechcloud-commons-iam
  template:
    metadata:
      labels:
        suite: mechcloud
        app: mechcloud-commons-iam
    spec:
      containers:
      - name: mechcloud-commons-iam
        image: {{ .Values.images.commonsIam }}
        envFrom:
        - configMapRef:
            name: mechcloud-python-common-configs
        - configMapRef:
            name: mechcloud-commons-services-configs
        ports:
        - containerPort: 5000
      restartPolicy: Always
      # this is required otherwise dns resolution will NOT work
      dnsConfig:
        options:
          - name: ndots
            value: "2"
      
      